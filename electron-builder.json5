// @see - https://www.electron.build/configuration/configuration
{
  $schema: "https://raw.githubusercontent.com/electron-userland/electron-builder/master/packages/app-builder-lib/scheme.json",
  appId: "com.dawlab.app",
  asar: true,
  productName: "DAWLab",
  publish: null, // Disable auto-publishing (handled by GitHub Actions)
  directories: {
    output: "release/${version}",
  },
  files: ["dist", "dist-electron"],
  mac: {
    target: [
      {
        target: "dmg",
        arch: ["x64", "arm64"],
      },
    ],
    icon: "src/assets/icons/logo.icns",
    artifactName: "${productName}-Mac-${arch}-${version}-Installer.${ext}",
    identity: null, // Explicitly disable code signing
    hardenedRuntime: false,
    gatekeeperAssess: false,
    entitlements: "entitlements.mac.plist",
    entitlementsInherit: "entitlements.mac.plist",
    protocols: [
      {
        name: "DAWLab",
        schemes: ["dawlab"],
      },
    ],
  },
  dmg: {
    sign: false, // Do not sign the DMG
    writeUpdateInfo: false,
    title: "${productName} ${version}",
    background: "src/assets/icons/dmg-background.tiff", // Retina 1x/2x packed TIFF (540x380 @1x)
    icon: "src/assets/icons/logo.icns", // mounted volume icon
    iconSize: 90,
    window: {
      width: 540,
      height: 380,
    },
    contents: [
      // x/y are icon CENTERS in the 540x380 window space; tuned to the drawn tiles
      { x: 128, y: 249, type: "file" }, // DAWLab app — left tile
      { x: 404, y: 245, type: "link", path: "/Applications" }, // right tile
    ],
  },
  win: {
    target: [
      {
        target: "nsis",
        arch: ["x64", "arm64"],
      },
    ],
    icon: "src/assets/icons/DAWLab_Logo.ico",
    artifactName: "${productName}-Windows-${arch}-${version}-Setup.${ext}",
  },
  nsis: {
    oneClick: false,
    perMachine: false,
    allowToChangeInstallationDirectory: true,
    deleteAppDataOnUninstall: false,
  },
  linux: {
    target: ["AppImage"],
    icon: "src/assets/logo.png",
    artifactName: "${productName}-Linux-${version}.${ext}",
  },
  protocols: [
    {
      name: "DAWLab",
      schemes: ["dawlab"],
    },
  ],
}
